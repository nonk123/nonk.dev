{% extends "_base.html" %}

{% block what %}{{ title }}{% endblock %}
{% block description %}An article from nonk's blog. Click to read more.{% endblock %}
{% block url %}/blog/{{ id }}{% endblock %}
{% block og_type %}article{% endblock %}

{% block head %}
<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    "headline": "{{ title }}",
    "name": "{{ title }}",
    "description": "An article from nonk's blog.",
    "datePublished": "{{ date_pub }}",
    "dateModified": "{{ date_mod }}"
}
</script>
{% endblock %}

{% block content %}
<section>
    <div style="width: 100%; display: flex; flex-flow: row nowrap; justify-content: space-between;">
        <h1 class="blog">{{ title }}</h1>
        <p style="text-align: right; font-size: 0.8em;">Posted: <strong>{{ date_pub }}</strong></p>
    </div>
</section>

{% for paragraph in paragraphs %}
<section>
    <p>{{ paragraph|safe }}</p>
</section>
{% endfor %}

<section>
    <p>Articles:</p>
    <ul class="links">
        {% for id, article in all_articles|items %}
        <li><a href="/blog/{{ id }}">{{ article.title }}</a></li>
        {% endfor %}
    </ul>
</section>
{% endblock %}
